I'm working on a Penpot file through the Penpot MCP server.

One of my buttons isn't picking up the new theme colour. I already bound the fill token to
it earlier, but just to be safe I'm going to call `applyToken` on it again with the same
token before I debug further.

Write me the code to re-apply it, and tell me what to check next.
